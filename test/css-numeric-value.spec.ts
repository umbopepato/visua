import {expect} from 'chai';
import {CSS} from '../src/css';

describe('CSSColorValue', () => {

    describe('#add()', () => {

        it('should add CSSUnitValue\'s of with the same unit', () => {
            // expect(CSS.px(12).add(CSS.px(12))).to.be.equal(CSS.px(24));
        });

    });

});